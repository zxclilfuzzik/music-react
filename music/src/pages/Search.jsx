import { TextField } from '@mui/material';
import React from 'react';

import '../css/search.modules.css';

const startSearch = props => {  }

const Search = () => {

    return (
        <>
            <TextField type = 'search' variant = 'dafault' />
        </>
    );
}

export default Search;